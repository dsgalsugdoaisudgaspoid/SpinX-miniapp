静态资源目录。

当前工程为减少外部依赖，界面视觉全部用 CSS 渐变 + emoji 实现，
未使用真实图片/图标；tabBar 采用纯文字模式，因此本目录暂为空。

上线前建议补充：
- 小程序图标 icon（120x120）
- tabBar 图标（可在 pages.json 的 tabBar.list 里补 iconPath / selectedIconPath）
- 品牌 Logo、活动海报占位图
